'use strict';

/* let uName = 'Kamal',
  age = 30,
  weight = null,
  newAge = age;

newAge = 32;

let names = ['Kamal', 'Gopi'];

let newNames = names;

newNames.push('Manu');

console.log(newNames, names);

console.log(uName + ' is a Developer');

console.log(`${uName} is a Developer and he is ${2022 - 1990} years old.`);

console.log(age, newAge); */

let userName = 'Kamal',
  age = Number('32') % 'as',
  isSingle = false + '',
  hasChildren = 1;

console.log(typeof userName, typeof age, typeof isSingle, age);

if (hasChildren) {
  console.log(true);
} else {
  console.log(false);
}
